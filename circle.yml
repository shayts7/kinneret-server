machine:
  services:
    - docker
dependencies:
  override: 
  - mvn clean install
test:
  override:
    - /bin/true
  post:
    - bash <(curl -s https://codecov.io/bash)
general:
  artifacts:
    - target/kinneret-server-1.0-SNAPSHOT.jar
